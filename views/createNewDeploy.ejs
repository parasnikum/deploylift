<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>All Deployments</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg: #0f1117;
      --card-bg: #1a1d24;
      --text: #f1f1f1;
      --muted: #999;
      --border: #2c2f36;
      --accent: #3b82f6;
      --accent-hover: #2563eb;
      --input-bg: #25272e;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg);
      color: var(--text);
      min-height: 100vh;
      display: flex;
    }

    /* Sidebar styles */
    aside.sidebar {
      width: 220px;
      background-color: var(--card-bg);
      padding: 2rem 1rem;
      border-right: 1px solid var(--border);
      height: 100vh;
      position: sticky;
      top: 0;
    }

    .sidebar h2 {
      font-size: 1.3rem;
      margin-bottom: 2rem;
      text-align: center;
      font-weight: 600;
      color: var(--accent);
    }

    .sidebar nav ul {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .sidebar nav a {
      color: var(--text);
      text-decoration: none;
      font-size: 1rem;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      transition: background-color 0.2s ease;
      display: block;
    }

    .sidebar nav a:hover {
      background-color: var(--border);
    }

    main {
      flex-grow: 1;
      padding: 2rem;
      overflow-y: auto;
    }

    h1 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 2.5rem;
      font-weight: 600;
    }

    .deploy-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .card {
      background-color: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 1.5rem;
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.3);
      transition: box-shadow 0.3s ease;
    }

    .card:hover {
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.4);
    }

    .card h3 {
      margin: 0;
      font-size: 1.2rem;
      color: var(--accent);
      font-weight: 600;
    }

    .card p {
      margin: 0;
      font-size: 0.92rem;
      color: var(--muted);
    }

    .card form {
      margin-top: auto;
    }

    .card input[type="submit"] {
      padding: 0.65rem 1rem;
      background-color: var(--accent);
      color: #fff;
      border: none;
      border-radius: 6px;
      font-size: 0.95rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 100%;
    }

    .card input[type="submit"]:hover {
      background-color: var(--accent-hover);
    }

    .upload-form {
      background-color: var(--card-bg);
      padding: 2rem;
      border-radius: 10px;
      max-width: 500px;
      margin: 0 auto;
      border: 1px solid var(--border);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
    }

    .upload-form h2 {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
    }

    .upload-form input[type="file"],
    .upload-form input[type="text"] {
      width: 100%;
      padding: 0.6rem;
      background-color: var(--input-bg);
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--text);
      font-size: 1rem;
      margin-bottom: 1.2rem;
    }

    .upload-form input[type="submit"] {
      width: 100%;
      padding: 0.75rem;
      background-color: var(--accent);
      color: #fff;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 500;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .upload-form input[type="submit"]:hover {
      background-color: var(--accent-hover);
    }

    @media (max-width: 960px) {
      aside.sidebar {
        display: none;
      }
    }

    @media (max-width: 600px) {
      .card input[type="submit"] {
        font-size: 0.9rem;
      }
    }

    .card-actions {
      display: flex;
      gap: 0.6rem;
      margin-top: auto;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-block;
      padding: 0.6rem 1rem;
      font-size: 0.95rem;
      font-weight: 500;
      text-align: center;
      border-radius: 6px;
      cursor: pointer;
      text-decoration: none;
      transition: background-color 0.3s ease, color 0.3s ease;
      border: none;
    }

    .btn.primary {
      background-color: var(--accent);
      color: #fff;
    }

    .btn.primary:hover {
      background-color: var(--accent-hover);
    }

    .btn.secondary {
      background-color: transparent;
      color: var(--accent);
      border: 1px solid var(--accent);
    }

    .btn.secondary:hover {
      background-color: var(--accent);
      color: #fff;
    }
  </style>
</head>

<body>

  <!-- Sidebar -->
  <aside class="sidebar">
    <h2>⚙️ Dashboard</h2>
    <nav>
      <ul>
        <li><a href="/">🏠 Home</a></li>
        <li><a href="/projects">📁 Projects</a></li>
        <li><a href="/newproject">➕ New Project</a></li>
        <li><a href="/analytics/<%= projectID %>">📊 Analytics</a></li>
        <li><a href="/login">🔐 Login</a></li>
        <li><a href="/register">📝 Register</a></li>
      </ul>
    </nav>
  </aside>

  <!-- Main Content -->
  <main>
    <h1>All Deployments</h1>

    <div class="deploy-grid">
      <% deploys.forEach(deployment=> { %>
        <div class="card">
          <h3>
            <%= deployment.deploymentName %>
          </h3>
          <p>Created: <%= new Date(deployment.createdAt).toLocaleString() %>
          </p>

          <div class="card-actions">
            <form action="/setDeploy/<%= projectID %>" method="post">
              <input type="hidden" name="deploymentID" value="<%= deployment.deploymentName %>">
              <button type="submit" class="btn primary">🚀 Deploy Now</button>
            </form>
            <a class="btn secondary" href="/<%= projectData.userID %>/preview/<%= deployment.deploymentName %>"
              target="_blank">👁️ Preview</a>
          </div>
        </div>
        <% }) %>
    </div>

    <div class="upload-form">
      <h2>Create New Deployment</h2>

      <form action="/newDeployment/<%= projectID %>" method="post" enctype="multipart/form-data">
        <label for="zip-upload">Upload .zip</label>
        <input id="zip-upload" accept=".zip" type="file" name="files" required />
        <button type="submit" class="btn primary">📦 Upload & Deploy</button>
      </form>

      <hr style="margin: 2rem 0; border-color: var(--border);">

      <form action="/newDeployment/repo/<%= projectID %>" method="post">
        <label for="repo-link">Deploy from Repo Link</label>
        <input id="repo-link" type="text" name="repo_link" placeholder="https://github.com/username/repo" />
        <button type="submit" class="btn primary">🔗 Deploy from Repo</button>
      </form>

      <hr style="margin: 2rem 0; border-color: var(--border);">

      <form action="/customDomainSet/<%= projectID %>" method="post">
        <label for="domain">Add Custom Domain</label>
        <input id="domain" type="text" name="domain" placeholder="yourdomain.com">
        <button type="submit" class="btn primary">🌐 Connect Domain</button>
      </form>
    </div>
  </main>




</body>

</html>