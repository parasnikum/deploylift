<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Analytics - <%= subdomain %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg: #0f1117;
      --card-bg: #1a1d24;
      --text: #f1f1f1;
      --muted: #999;
      --border: #2c2f36;
      --accent: #3b82f6;
      --accent-hover: #2563eb;
      --input-bg: #25272e;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg);
      color: var(--text);
      min-height: 100vh;
      display: flex;
    }

    /* Sidebar styles */
    aside.sidebar {
      width: 220px;
      background-color: var(--card-bg);
      padding: 2rem 1rem;
      border-right: 1px solid var(--border);
      height: 100vh;
      position: sticky;
      top: 0;
    }

    .sidebar h2 {
      font-size: 1.3rem;
      margin-bottom: 2rem;
      text-align: center;
      font-weight: 600;
      color: var(--accent);
    }

    .sidebar nav ul {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .sidebar nav a {
      color: var(--text);
      text-decoration: none;
      font-size: 1rem;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      transition: background-color 0.2s ease;
      display: block;
    }

    .sidebar nav a:hover {
      background-color: var(--border);
    }

    main {
      flex-grow: 1;
      padding: 2rem;
      overflow-y: auto;
    }

    h1 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 2.5rem;
      font-weight: 600;
    }

    .analytics-card {
      max-width: 500px;
      margin: 0 auto;
      background-color: var(--card-bg);
      padding: 2rem;
      border-radius: 10px;
      border: 1px solid var(--border);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
      text-align: center;
    }

    .analytics-card h2 {
      color: var(--accent);
      margin-bottom: 1.5rem;
      font-weight: 600;
      font-size: 1.8rem;
    }

    .analytics-card p {
      font-size: 1.2rem;
      color: var(--text);
      margin-bottom: 1rem;
    }

    .analytics-card .highlight {
      font-weight: 700;
      font-size: 2.4rem;
      color: var(--accent-hover);
    }

    @media (max-width: 960px) {
      aside.sidebar {
        display: none;
      }
    }
  </style>
</head>

<body>

  <!-- Sidebar -->
  <aside class="sidebar">
    <h2>⚙️ Dashboard</h2>
    <nav>
      <ul>
        <li><a href="/">🏠 Home</a></li>
        <li><a href="/projects">📁 Projects</a></li>
        <li><a href="/newproject">➕ New Project</a></li>
        <li><a href="/analytics/<%= projectId %>">📊 Analytics</a></li>
        <li><a href="/login">🔐 Login</a></li>
        <li><a href="/register">📝 Register</a></li>
      </ul>
    </nav>
  </aside>

  <!-- Main Content -->
  <main>
    <h1>Analytics Dashboard</h1>

    <div class="analytics-card">
      <h2>Project: <%= subdomain %></h2>
      <p>Live Visitors:</p>
      <p class="highlight"><%= liveVisitors %></p>
    </div>
  </main>

</body>

</html>
